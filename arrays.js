const fruits = ['apple', 'banana','orange']
let indexOfApp = fruits.indexOf('apple')

//console.log(indexOfApp)


const arr = [12,3,4,5,6]
//const traget = 3

//let res = arr.indexOf(5)

if (res !== -1){
     console.log(true)
    }
    
    function containSuspect(arr, target){
        return arr.indexOf(target) !== -1 // true or false
    }

function containSuspect_2(arr, target){
    return arr.indexOf(target) !== -1
}

console.log("Did we find the culprit", containSuspect(input, 1000))

let check = (0.1 + 0.2) !== 0.3

console.log(check)
